" File type plugin for TeX based files.
" Author: Tim Baccaert <github.com/timutable>

" Vimtex plugin settings.
let g:vimtex_enabled = 1
let g:vimtex_view_method = 'skim'
let g:tex_flavor = 'latex'

" Enable spellchecker.
setlocal spell
setlocal colorcolumn=80
